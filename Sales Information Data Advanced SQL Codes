--SQL  JOINS--
 

SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
      ,O.[Customer_ID]
      ,O.[Salesman_id]
      ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,S.[city]
      ,S.[Name]
      ,S.[commission]
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
 

--Question 1: write a SQL query to find the salesperson and customer who reside in the same city. Return Salesman, cust_name and city.

 SELECT
      C.[Customer_Name]
      ,C.[City]
      
      ,S.[city]
      ,S.[Name]
      
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE C.[City]=S.[city]
 
--Question 2: write a SQL query to find those orders where the order amount exists between 500 and 2000. Return ord_no, purch_amt, cust_name, city.

SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,C.[Customer_Name]
      ,C.[City]
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE Purchase_amount BETWEEN 500 AND 2000

--Question 3: write a SQL query to find the salesperson(James Hoog) and the customer(s) he represents. Return Customer Name, city, Salesman, commission.

SELECT C.[Customer_Name]
      ,C.[City]
      ,S.[Name]
 
      ,S.[commission]
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE NAME='James hoog'

--Question 4: write a SQL query to find salespeople who received commissions of more than 12 percent from the company. Return Customer Name, customer city, Salesman, commission.  

SELECT C.[Customer_Name]
      ,C.[City]
      
      ,S.[Name]
      ,S.[commission]
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE commission>'00:12:00.0000000'

--Question 5: write a SQL query to locate those salespeople who do not live in the same city where their customers live and have received a commission of more than 12% from the company. Return Customer Name, customer city, Salesman, salesman city, commission.  


SELECT C.[Customer_Name]
      ,C.[City]
      
      ,S.[city]
      ,S.[Name]
     
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE C.[City]!=S.[city] AND commission>'00:12:00.0000000'

--Question 6: write a SQL query to find the details of an order(>2000). Return ord_no, ord_date, purch_amt, Customer Name, grade, Salesman, commission

SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
      
      
      ,C.[Customer_Name]
      
      ,C.[Grade]
      
      ,S.[Name]
      ,S.[commission]
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE Purchase_amount>'2000'

--Question 7: Write a SQL statement to join the tables salesman, customer and orders so that the same column of each table appears once and only the relational rows are returned. 

SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
      ,O.[Customer_ID]
      ,O.[Salesman_id]
      ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,S.[city]
      ,S.[Name]
      ,S.[commission]
  FROM [dbo].[Order] As O
  JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 

  --Question 8: write a SQL query to display the customer name, customer city, grade, salesman, salesman city. The results should be sorted by ascending customer_id.

  SELECT O.[Customer_ID]
      
      
      
      
       ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,S.[city]
      ,S.[Name]
      
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID
  ORDER BY O.[Customer_ID] ASC

  --Question 9: write a SQL query to find those customers with a grade less than 300. Return cust_name, customer city, grade, Salesman, salesmancity. The result should be ordered by ascending customer_id.

  SELECT O.[Customer_ID]
       ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,S.[city]
      ,S.[Name]
      
  FROM [dbo].[Order] As O
  JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE Grade<'300'
  ORDER BY O.[Customer_ID] ASC

--Question 10: Write a SQL statement to make a report with customer name, city, order number, order date, and order amount in ascending order according to the order date to determine whether any of the existing customers have placed an order or not.

SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
     
      ,C.[Customer_Name] 
      ,C.[City]
      
  FROM [dbo].[Order] As O
  JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  ORDER BY O.[Order_Date] ASC

--Question 11: SQL statement to generate a report with customer name, city, order number, order date, order amount, salesperson name, and commission to determine if any of the existing customers have not placed orders or if they have placed orders through their salesman or by themselves.

 SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
     
      ,C.[Customer_Name]
      ,C.[City]
     
      ,S.[Name]
      ,S.[commission]
  FROM [dbo].[Order] As O
  JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 

  --Question 12: Write a SQL statement to generate a list in ascending order of salespersons who work either for one or more customers or have not yet joined any of the customers.

  
  SELECT
      S.[Name], C.[Customer_Name]
      ,COUNT(S.[Name])
  FROM [dbo].[Order] As O
  JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  GROUP BY  S.[Name], C.[Customer_Name] 
  ORDER BY  S.[Name] ASC

  13--write a SQL query to list all salespersons along with customer name, city, grade, order number, date, and amount.
 
     SELECT
      S.[Name]
       ,O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
      
      ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
     
      
      
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 

--Question 14: Write a SQL statement to make a list for the salesmen who either work for one or more customers or yet to join any of the customer. The customer may have placed, either one or more orders on or above order amount 2000 and must have a grade, or he may not have placed any order to the associated supplier.


SELECT DISTINCT
      
       O.[Salesman_id]
      
      ,S.[city]
      ,S.[Name]
      
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
  WHERE
    
        C.[Grade] IS NOT NULL
        AND Purchase_Amount >= '2000' OR O.[Order_Number] IS NULL
    
    OR O.[Customer_ID] IS NULL

--Question 15: For those customers from the existing list who put one or more orders, or which orders have been placed by the customer who is not on the list, create a report containing the customer name, city, order number, order date, and purchase amount

SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
      
      ,C.[Customer_Name]
      ,C.[City]
      
      ,S.[city]
      
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID 
 WHERE C.Customer_ID >='1' OR C.Customer_ID  is NULL

--Question 16: Write a SQL statement to generate a report with the customer name, city, order no. order date, purchase amount for only those customers on the list who must have a grade and placed one or more orders or which order(s) have been placed by the customer who neither is on the list nor has a grade.

 SELECT O.[Order_Number]
      ,O.[Purchase_Amount]
      ,O.[Order_Date]
      
      ,C.[Customer_Name]
      ,C.[City]
      
      ,S.[city]
      
  FROM [dbo].[Order] As O
  LEFT JOIN [dbo].[Customer] As C
  ON O.Customer_ID=C.Customer_ID 
  LEFT JOIN [dbo].[Salesman] As S
  ON O.[Salesman_id]=S.Salesman_ID
  WHERE C.[Grade] IS NOT NULL AND C.Customer_ID>='1' 
  OR C.Customer_ID IS NULL 
  OR C.[Grade] IS NULL

--Question 17: Write a SQL query to combine each row of the salesman table with each row of the customer table.

SELECT S.[Salesman_id]
      ,S.[Name]
      ,S.[city]
      ,S.[commission]
      ,C.[Customer_ID]
      ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,C.[Salesman_ID]
  FROM [dbo].[Salesman] As S
  CROSS JOIN [dbo].[Customer] As C

--Question 18: Write a SQL statement to create a Cartesian product between salesperson and customer, i.e. each salesperson will appear for all customers and vice versa for that salesperson who belongs to that city.

SELECT S.[Salesman_id]
      ,S.[Name]
      ,S.[city]
      ,S.[commission]
      ,C.[Customer_ID]
      ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,C.[Salesman_ID]
  FROM [dbo].[Salesman] As S
  CROSS JOIN [dbo].[Customer] As C
  WHERE S.[city]=C.[City]

--Question 19: Write a SQL statement to create a Cartesian product between salesperson and customer, i.e. each salesperson will appear for every customer and vice versa for those salesmen who belong to a city and customers who require a grade.

SELECT S.[Salesman_id]
      ,S.[Name]
      ,S.[city]
      ,S.[commission]
      ,C.[Customer_ID]
      ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,C.[Salesman_ID]
  FROM [dbo].[Salesman] As S
  CROSS JOIN [dbo].[Customer] As C
  WHERE S.[city]=C.[City] AND C.[Grade] IS NULL

--Question 20: Write a SQL statement to make a Cartesian product between salesman and customer i.e. each salesman will appear for all customers and vice versa for those salesmen who must belong to a city which is not the same as his customer and the customers should have their own grade.

SELECT S.[Salesman_id]
      ,S.[Name]
      ,S.[city]
      ,S.[commission]
      ,C.[Customer_ID]
      ,C.[Customer_Name]
      ,C.[City]
      ,C.[Grade]
      ,C.[Salesman_ID]
  FROM [dbo].[Salesman] As S
  CROSS JOIN [dbo].[Customer] As C
  WHERE S.[city]!=C.[City] AND C.[Grade] IS NOT NULL
